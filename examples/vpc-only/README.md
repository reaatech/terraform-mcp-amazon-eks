# VPC-Only Example

This example consumes the root module with a private Kubernetes API endpoint and Multi-AZ Redis. Supply private subnets explicitly; unlike the basic example, this one is intended for non-default-network production-style environments.
