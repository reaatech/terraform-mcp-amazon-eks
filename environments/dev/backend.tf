terraform {
  # Uncomment and configure for remote state
  # backend "s3" {
  #   bucket = "your-tfstate-bucket"
  #   key    = "terraform-mcp-aws-eks/dev/terraform.tfstate"
  #   region = "us-west-2"
  # }
}
